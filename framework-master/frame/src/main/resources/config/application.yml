mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  mapper-locations: classpath:mapper/*.xml

shiro:
  loginUrl: index.html

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: tacbin@123
    username: root
#    url: jdbc:mysql://localhost:3306/frame?useUnicode=true&characterEncoding=utf8&autoReconnect=true&failOverReadOnly=false&allowMultiQueries=true&useSSL=true&serverTimezone=Asia/Shanghai
  redis:
    host: 127.0.0.1
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        max-wait: -1
        min-idle: 0
    port: 6379
    timeout: 5000
    password: tacbin@123
  # 文件大小
  servlet:
    multipart:
      max-file-size: 4MB
      max-request-size: 4MB
      enabled: true
custom:
  md-times: 2

